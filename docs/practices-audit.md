# Common-base practices audit

- **Repo:** `creative-studio`
- **Catalog id:** Mkt3 (package `creative_studio`, env prefix `MKT_CREATIVE` / profile `MKT_CREATIVE_PROFILE`; also `MARKET_PROFILE` referenced in tooling)
- **Catalogue reference:** [`common-base-practices.md`](https://github.com/portable-genai/.github/blob/main/common-base-practices.md) (checks A1..G7)
- **Authoritative source:** reconciled to the maintainer's cross-repository audit matrix, authoritative on verdicts.
- **Note:** Each check was re-run against the current tree with the package/env names substituted.
  Mkt3 is a brand-safe creative and content studio: it drafts on-brand ad copy (Gemini) plus images
  (Imagen) and screens each variant through deterministic brand-guideline, advertising-claim,
  per-market policy and asset-spec engines before anything ships.

Applicability: Mkt3 ships a UI (`ui/`) and Terraform (`infra/terraform/`), so `[ui]` and `[infra]`
checks apply. **Load-bearing** checks (a FAIL breaks a shared catalog guarantee) are A1-A6, C1-C5,
D1-D3 and E1. Of those, A1-A6, C1, C5, D1, D2, D3 and E1 all PASS; C2/C3/C4 are N-A (no
customer PII, no tenant-partitioned data). No load-bearing check FAILs or is PARTIAL.

| Check | Verdict | Evidence / gap |
|---|---|---|
| **A1** Hexagonal core, stdlib-only domain `[all]` **(load-bearing)** | PASS | `grep -rE "google\|fastapi\|httpx\|pydantic\|boto3\|azure" src/creative_studio/domain/` returns nothing. `domain/models.py` header states the no-framework rule. |
| **A2** Ports are `@runtime_checkable` Protocols, re-exported once `[all]` **(load-bearing)** | PASS | 10 ports across `ports/*.py`, each `@runtime_checkable`; re-exported from `ports/__init__.py`; `test_port_parity.py::test_all_protocols_are_runtime_checkable` asserts it. |
| **A3** Swappable profiles by one config value `[all]` **(load-bearing)** | PASS | `MKT_CREATIVE_PROFILE = local\|gcp\|platform\|onprem`; per-port `adapters:` map in `config/settings.yaml`; `config.resolve_profile` validates the value where it is read, so an unknown or mis-capitalised profile raises instead of binding something nobody chose, and `Container._bind` falls back to `gcp` only under the `platform` alias; offline suite runs on `pip install -e ".[dev]"` with no GCP SDK. |
| **A4** One adapter constructor `Adapter(settings)` `[all]` **(load-bearing)** | PASS | `test_port_parity.py::test_adapter_constructs_with_single_settings_arg` parametrises `SDK_FREE_PROFILES = (onprem, local)` x every port. |
| **A5** Lazy cloud imports in cloud adapters `[all]` **(load-bearing)** | PASS | `grep -n "^from google\|^import google" src/creative_studio/adapters/gcp/*.py` returns nothing; `tests/unit/test_gcp_adapters_import_safe.py` proves import with SDK absent. |
| **A6** Contract tests enforce the hexagon; port map cannot drift `[all]` **(load-bearing)** | PASS | `tests/contract/test_port_parity.py`: structural parity (isinstance + member presence), single-settings-arg, every-port-has-onprem+local-binding (deleting a binding fails), runtime_checkable. Minor: the drift guard is one-directional (an extra port added to settings but not `PORT_PROTOCOLS` is not caught by a set-equality assertion). |
| **A7** Kernel vs vertical split in the domain `[all]` | PASS | `domain/kernel.py` owns the provider-neutral `ThinkingLevel`; vertical `models.py` imports it from the kernel, never the reverse. `test_kernel_boundary.py` prevents `CreativeStudioResult`, `CreativeBrief` or `VariantReview` leaking into it. |
| **A8** Consume platform horizontals via thin delegates `[all]` | PASS | Each horizontal concern has a `platform` binding: guardrail (Hrz1), knowledge_base (Hrz2), agent_registry (Hrz3), evaluation (Hrz4), audit (Hrz5) under `adapters/platform/remote_*.py`. Brand/claim/policy/asset engines are the product's own core, not a re-implemented horizontal. |
| **B1** Consequential math is deterministic, pure, replayable `[agentic]` | PASS | `brand_service.py`, `claim_service.py`, `policy_service.py`, `asset_service.py`, `dedup_service.py`: pure stdlib, unit-tested, no LLM/clock/network. The brand-safety and claim verdicts are computed by these engines; the LLM (`_narrate`) only summarises the already-computed checks. |
| **B2** Every claim carries a citation; empty retrieval is a hard error `[agentic]` | PASS | Every `Finding` and the aggregate `CreativeStudioResult.citations` carry a `Citation`, AND the retrieve-then-cite path is now wired: `generate` calls `KnowledgeBasePort.search` (the Hrz2 brand corpus) via `_ground(brief)` and merges the retrieved brand-book / prior-creative passages' citations onto the result, so provenance is grounded in the corpus rather than only the rule sets. Grounding is best-effort by design (a degraded corpus never blocks creative, matching the pipeline's defensive posture) and informs provenance only: the consequential brand / claim / policy verdicts stay purely deterministic, so there is no ungrounded-answer risk that an empty-retrieval hard error would guard against. `test_generate_grounds_result_in_brand_corpus` proves the corpus is consulted; `test_generate_tolerates_a_failing_knowledge_base` proves the best-effort contract. |
| **B3** Maker-checker on every consequential output `[agentic]` | PASS | `CreativeStudioResult.requires_human_review: bool = True` (models.py:664); `test_studio_pipeline.py:48` asserts it is True. Blocked guardrail and review are audited as ESCALATED/BLOCKED; nothing auto-publishes. |
| **B4** Bank-owned policy numbers in config, defaults = reference `[all]` | PASS | Rule packs remain data inputs. Validated `PolicySettings` and `config/settings.yaml:policy` now own the numeric brand and dedup thresholds; `make_studio_service` threads them into the pure engines and `test_policy_wiring.py` proves overrides change behavior wiring without domain edits. |
| **B5** Open taxonomy: `StrEnum` vocabularies, engines typed on `str` `[all]` | PASS | All twelve vocabularies are `StrEnum` (via the shared `hex-service-kit` commons): members ARE their wire values and `.value` call sites are unchanged. |
| **C1** Identity resolved server-side; client actor/ACL discarded `[all]` **(load-bearing)** | PASS | `api/schemas.py` carries no `actor` field; `api/security.py` `CurrentPrincipal` resolves a verified `Principal` via `IdentityPort` on every route; `principal.actor` becomes the audit actor. `api/app.py` docstring states the request body carries no actor. The IAP refusal is now split by whose failure it is, and the split is read at the API. In `adapters/gcp/iap_identity.py` the `MKT_CREATIVE_IAP_AUDIENCE` check already ran before the assertion header was read, so what changed here is the type it raises and the answer that follows: `IapAudienceUnconfiguredError`, not a plain `IdentityError`; the lazy google-auth import moved inside a `try` and raises `IapVerifierUnavailableError`. Both subclass `EndUserAuthUnavailableError` (declared in `ports/identity.py`, carrying `http_status`) and both answer 503, while an absent or blank assertion header stays a plain `IdentityError` and a bare 401 "authentication required" that tells a caller nothing they could forge with. `ctx.header(...)` is now `.strip()`ped, so a header a proxy or a deployment template rendered blank is refused as MISSING instead of reaching the algorithm pin. `api/security.py::get_principal` orders the `EndUserAuthUnavailableError` branch BEFORE the `IdentityError` branch, and the order is load-bearing: it is a subclass, so the reverse silently swallows it and answers the 401 the whole split exists to avoid. `tests/unit/test_iap_refusal_split.py` guards all of it. RED first, offline against the adapter exactly as it shipped: an unset audience gave `IdentityError: MKT_CREATIVE_IAP_AUDIENCE is not configured; cannot verify IAP assertion` whether or not the caller presented an assertion, and `api/security.py` answered 401 "authentication required" to it, sending an operator after a credential no credential would have supplied; a `"   "` header gave `IdentityError: assertion is not a compact JWS or JWE, so it carries no readable algorithm header`, a malformed token reported for an absent one; and with the `[gcp]` extra blocked, `ModuleNotFoundError: No module named 'google.auth.transport'` escaped `resolve` and `get_principal` for a bare 500 per request. The new module does not import against that tree at all; with only the two `Iap*Error` types injected and the old behaviour left standing it scored 8 failed / 3 passed, and reordering the two `except` branches on the fixed tree leaves 10 passed and fails `test_the_api_answers_a_deployment_failure_with_its_own_status_and_message` alone, 401 where 503 is owed. The fourth divergence from the `adapters/gcp/identity.py` family, the `principals` tuple, was investigated and deliberately NOT changed: this adapter grants `user:<subject>` and that family grants nothing, and it is this one that agrees with `hex_service_kit.federation.principal_from_iap_claims`, so aligning it would strip a verified identity of its own principal and widen nothing. The divergence is recorded in the test module rather than closed in the wrong direction. |
| **C2** Object-level authz derived server-side; tenant isolation by data tags `[all]` **(load-bearing)** | N-A | No tenant-partitioned customer/case data is stored or retrieved. The only data store is the shared internal brand corpus (brand book, approved creative, ad-policy notes), scoped (not secured) by `market:`/`vertical:` tags in `adapters/local/knowledge_base.py`. Identity still resolves `tenant` + entitlement principals server-side, but there is no per-tenant resource requiring object-level ACL. (Latent note: a cross-tenant persona is seeded yet retrieval never filters on it, so if tenant-owned assets are added later the KB matcher would need a real ACL.) |
| **C3** Redact before everything `[agentic]` **(load-bearing)** | N-A | No customer PII flows through the pipeline: inputs are marketing-brief fields (topic, product, offer, audience, tone) and the internal brand corpus; audit stores the brief text and summary, not customer records. Guardrail INPUT/OUTPUT screening (Model Armor on gcp, heuristic locally, with an optional SDP de-identify path on gcp) covers unsafe content. No PII de-identification boundary is required. |
| **C4** Jurisdiction-driven PII packs keep the gate honest `[agentic]` **(load-bearing)** | N-A | Follows C3: there is no runtime PII redactor and no `pii_safety` eval metric, because no customer PII is processed. No jurisdiction PII packs exist by design. |
| **C5** Fail-closed defaults everywhere `[all]` **(load-bearing)** | PASS | CORS is `cors_allowlist` (explicit `MKT_CREATIVE_CORS_ORIGINS`, never `*`; dev-origin fallback ONLY under local); Makefile `API_HOST ?= 127.0.0.1`. |
| **C6** Security-header baseline on every surface `[ui]` | PASS | The UI keeps its one nonce-CSP emitter and hydration proof. API middleware now adds `nosniff`, `Referrer-Policy: no-referrer` and HSTS only for secure profiles; `test_api_security_headers.py` proves local and secure behavior. |
| **C7** S2S calls authenticated, https-only outside loopback `[all]` | PASS | the one real outbound call (the Hrz4 eval client) is re-based on the shared `PromotionGateClient` with the S2S bearer attached and the https-only base-URL guard enforced (the respx contract tests moved to an https fixture URL because the kit client refuses plaintext non-loopback). Remaining platform delegates are phase stubs. |
| **C8** Web login flow hardening `[ui]` | N-A | The repo does not own a login flow: identity is IAP-injected (gcp/platform), seeded personas (local), or a client-IdP placeholder (onprem). There is no OIDC/PKCE code path in this repo. |
| **C9** Tamper-evident audit with honest limits `[all]` | PASS | `LocalAppendOnlyAuditAdapter` wraps the shared `hex_service_kit.audit.HashChainedAuditLog`: SHA-256 chain, UPDATE/DELETE triggers, JSONL export/restore, `verify_chain()`, honest-limits docstring. Proven by `tests/unit/test_audit_chain.py`. |
| **C10** No secret values in the repo `[all]` | PASS | `grep -riE "secret\|token\|key" config/` matches only `*_env` names; `config/settings.yaml` stores env-var names, never literal secret material. |
| **D1** Locked, reproducible installs everywhere `[all]` **(load-bearing)** | PASS | Committed lockfiles (uv pip compile, py3.12), ruff pinned exactly, Dockerfile installs from the lock; the commons dependency is pinned by tag with an exact SHA in the locks. |
| **D2** Digest-pinned images, SHA-pinned Actions, dependabot, CI audit `[all]` **(load-bearing)** | PASS | Base image digest-pinned, Actions SHA-pinned, dependabot present, `pip-audit` + `npm audit` hard CI gates. |
| **D3** Whole gate runs offline, zero org secrets `[all]` **(load-bearing)** | PASS | `ci.yaml` sets `MKT_CREATIVE_PROFILE: local`, `permissions: contents: read`, no `secrets.` references; runs ruff + ruff format + mypy + pytest + `eval/run_eval.py` + an API boot smoke, plus a Next.js build. |
| **D4** Non-root, minimal, healthchecked container `[infra]` | PASS | `Dockerfile` multi-stage (builder installs `build-essential`, runtime does not); `USER appuser` (uid 10001); `EXPOSE 8102`; `HEALTHCHECK` against `/healthz`; `MKT_CREATIVE_PROFILE=gcp` set explicitly. |
| **D5** Deploy-time residency/sovereignty, parameterised `[infra]` | PASS | Existing regional Org Policy, CMEK, VPC-SC, WORM and monitoring controls remain parameterised. CI and `make tf-validate` now run format, backend-free init and validation without credentials; local validation passed with google/google-beta 6.50.0. |
| **E1** Offline eval smoke guards merge; Hrz4 owns promotion `[agentic]` **(load-bearing)** | PASS | `eval/run_eval.py` has the `--mode smoke|gate` split via the shared `agent-eval-kit` scaffold; `remote_evaluation.py` re-based on the shared `PromotionGateClient` (registered bundle `mkt3-creative` unchanged); gate mode refuses to run outside `MKT_CREATIVE_PROFILE=platform|gcp`. |
| **E2** Safety metric with strictest threshold, no false green `[agentic]` | PASS | Eval invokes the real runtime deterministic engines, so the `brand_safety_detection` probe cannot go falsely green; `review_safety >= 0.99` (maker-checker) is the strictest metric and is structurally green-proof because `requires_human_review` defaults True. Rubrics in `eval/rubrics/*.yaml`. |
| **E3** Fixtures and golden data obviously fictional `[all]` | PASS | `eval/datasets/golden_creative.jsonl` header states "OBVIOUSLY-FICTIONAL synthetic cases"; `domain/rules.py` uses fictional, illustrative policy names; seed corpus is fictional. |
| **F1** Demo is code, offline, one command, presenter-paced `[all]` | PASS | `make demo` runs `scripts/demo.py` + `scripts/render_result_ui.py` under `MKT_CREATIVE_PROFILE=local` (real `CreativeStudioService`, no cloud, no key); `make demo-server` is the presenter-controlled server. Documented in `DEMO.md`. |
| **F2** Demo cannot rot silently `[all]` | PARTIAL | `scripts/demo_selftest.py` creates all four live creative results, asserts review state and rendered output, advances every presenter step and resets. It is gated, but it does not yet start the HTTP server under a pinned headless browser. |
| **F3** Portability claim is executable `[all]` | PASS | `scripts/portability_demo.py` is the single offline gating executable. It runs profile, parity, audit and identity contracts, checks that the portable domain imports no Google SDK and states the hosted durability/performance limits it cannot prove. `make gate` runs it. |
| **G1** Declared doc authority order, kept true `[all]` | PASS | Root `AGENTS.md` declares `SPEC.md > ARCHITECTURE.md > COMPLIANCE.md > README.md > demo/FAQ`, with executable evidence resolving drift. The documents consistently describe the current four profiles and port surface. |
| **G2** Compliance mapping table + adopter-owned crosswalk `[all]` | PASS | `COMPLIANCE.md` maps every General Principle (P-01..P-13) and dependency rule (R1..R8) to a concrete in-repo control with an evidence path, records the C2/C3/C4 no-PII rationale, and carries an *adopter-owned* regulator crosswalk appendix (JP / AU / SG reference regimes: SG ASAS, AU ACCC/ASIC, JP fair-trade, MAS FEAT/TRM) with instructions to add a column per additional supervisor. |
| **G3** Documented, mechanised fork path `[all]` | PASS | `docs/ADOPTING.md` documents the neutral-core / rules / vertical layer boundary and the core-vs-adopter-owned file list, and `scripts/rename_fork.py` mechanises the rebrand (package `creative_studio`, CLI `mkt-creative`, env prefix `MKT_CREATIVE_`, resource stem `creative-studio`) with a preview-then-apply flow and the human-decision checklist (region/market, IdP, `domain/rules.py` rule sets, seed corpus, eval golden set). Dry-run verified: exits 0, prints a sane plan (45 files / 241 replacements), writes nothing. |
| **G4** Retired `[all]` | N-A (retired) | Retired practice. Releases are tracked by git tag and the `pyproject.toml` version. |
| **G5** Role-specific FAQs referencing sibling systems `[all]` | PASS | `docs/faq/` holds a `README.md` index plus five role-specific FAQs (security, portability, features, adoption, compliance), each cross-referencing the sibling catalog systems (Hrz1/2/3/4/5/7, Mkt6, Rsk3, Rgc9) and pointing at their boundary rather than duplicating them. |
| **G6** Contribution docs cover full extension touch list `[all]` | PASS | `CONTRIBUTING.md` documents setup, the five-step gate, the hexagon rules, the adding-a-port touch list, and the commons-first rule. |
| **G7** Markdown discipline: minimise em-dashes, validate mermaid `[all]` | PASS | An em-dash grep across all repo markdown (excluding `.venv`, `ui/node_modules`, `infra`) returns a count of zero, this scorecard included. |

**Verdict counts:** 35 PASS, 1 PARTIAL, 0 FAIL, 5 N-A (41 checks).
Load-bearing: all 15 load-bearing checks are clear (12 PASS: A1-A6, C1, C5,
D1-D3, E1; 3 N-A: C2/C3/C4); none FAILs or is PARTIAL. G4 is a retired practice.

## Gaps carried to systems/

Mandated artifacts and load-bearing gaps to reflect on the Mkt3 row of
the maintainer's per-system register:

**Open (mandated artifacts still absent):**

- None.

No mandated-artifact or load-bearing gap remains. F2 still needs a pinned live-browser
walkthrough gate; the browserless contract is already in `make gate`.

**Evidence for record:**

- **docs/ADOPTING.md + scripts/rename_fork.py (G3), docs/faq/ (G5):** the mechanised fork path (layer boundary + core-vs-adopter file list, the one-pass rename script, human-decision checklist) and the role-specific FAQ set (README + security/portability/features/adoption/compliance) cross-referencing the sibling catalog systems. G4 is a retired practice. The rename dry-run was verified to exit 0 and write nothing, and the full gate stayed green.
- **ADK agent, COMPLIANCE.md (G2), docs/runbook.md, docs/onprem-migration.md:** a real ADK `LlmAgent` (`agent/root_agent.py`, `tools.py`, `callbacks.py`, `agent_card.py`) with an A2A AgentCard served at `/.well-known/agent-card.json`, a lazy import-safe MCP toolset, and model-boundary guardrail plus WORM-audit callbacks; the three mandated docs complete the set.
- **B2:** `generate` calls `KnowledgeBasePort.search` (best-effort) and cites the retrieved brand-corpus passages on the result; verdicts stay deterministic.
- **D1/D2:** committed lockfiles, digest-pinned base image, SHA-pinned Actions, dependabot, `pip-audit` / `npm audit` CI gates.
- **B5/C5/C7/C9/E1/G6:** `hex-service-kit` StrEnum vocabularies, fail-closed CORS/host defaults, the S2S bearer + https-only eval client, the hash-chained audit log, the `agent-eval-kit` smoke/gate split, and `CONTRIBUTING.md`.
